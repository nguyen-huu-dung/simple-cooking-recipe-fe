export const ENVIRONMENT_MODE = import.meta.env.VITE_ENVIRONMENT;

export const URL_CONFIG = {
    API: {
        url: import.meta.env.VITE_API_URL
    },
    SOCKET: {
        url: import.meta.env.VITE_SOCKET_URL
    }
};
